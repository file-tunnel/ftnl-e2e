# Security policy

Report vulnerabilities using GitHub Security Advisories.

Never run this suite against production user tunnels. Traces, videos, and
screenshots can contain one-time pairing links or filenames even when the
suite avoids logging them. CI must use an isolated environment with synthetic
files and short artifact retention.
